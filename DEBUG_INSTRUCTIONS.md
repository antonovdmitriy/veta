# Инструкция по отладке подсветки синтаксиса

## Шаг 1: Проверьте тестовый view

Я создал специальный тестовый экран `TestHighlightView.swift` который показывает подсветку напрямую без markdown.

### Как открыть:

1. В Xcode откройте файл `TestHighlightView.swift`
2. В правой панели нажмите "Canvas" (или Editor → Canvas)
3. Вы должны увидеть Preview с подсветкой кода для Java, Swift и Python

**Если вы видите цветной код в Preview - значит Highlightr работает!**

### Альтернатива - временно замените ContentView:

В файле `mindpalaceApp.swift` замените:
```swift
ContentView()
```

на:
```swift
TestHighlightView()
```

Запустите приложение - вы увидите тестовый экран с подсветкой.

---

## Шаг 2: Проверьте консоль

Когда вы открываете markdown файл с кодом, в консоли Xcode должны появиться сообщения:

```
📦 HighlightedCodeBlock создан! Язык: java
🎨 CodeHighlighter вызван! Язык: java, код: public class HelloWorld {...
✅ Подсветка работает! Атрибутов: 150
```

### Как открыть консоль:

1. В Xcode: View → Debug Area → Activate Console (или Cmd+Shift+C)
2. Запустите приложение
3. Откройте markdown файл с блоком кода
4. Смотрите на вывод консоли

### Что означают сообщения:

- ✅ `📦 HighlightedCodeBlock создан` - наш компонент загружается
- ✅ `🎨 CodeHighlighter вызван` - highlighter вызывается
- ✅ `✅ Подсветка работает` - Highlightr успешно подсветил код
- ❌ `❌ Highlightr вернул nil` - проблема с Highlightr

---

## Шаг 3: Проверьте формат блока кода в markdown

Убедитесь, что в вашем markdown файле блоки кода правильно оформлены:

### ✅ Правильно:

\`\`\`java
public class Test {
    public static void main(String[] args) {
        System.out.println("Hello");
    }
}
\`\`\`

### ✅ Тоже правильно (другие языки):

\`\`\`python
def hello():
    print("Hello")
\`\`\`

\`\`\`javascript
function hello() {
    console.log("Hello");
}
\`\`\`

\`\`\`kotlin
fun main() {
    println("Hello")
}
\`\`\`

### ❌ Неправильно:

```
// Без указания языка
public class Test {}
```

---

## Возможные проблемы и решения

### 1. В консоли ничего не появляется

**Проблема:** `HighlightedCodeBlock` не вызывается

**Решение:**
- Проверьте, что файлы добавлены в target (правая панель → Target Membership)
- Пересоберите проект: Product → Clean Build Folder → Product → Build

### 2. Появляется "❌ Highlightr вернул nil"

**Проблема:** Highlightr не может подсветить этот язык

**Решение:**
- Проверьте название языка в блоке кода (должно быть `java`, а не `Java`)
- Попробуйте другие языки: swift, python, javascript, kotlin, go

### 3. Код белый, но консоль показывает успех

**Проблема:** NSAttributedString теряет цвета при рендеринге

**Решение:** Это уже исправлено через `AttributedText` с `UILabel`

### 4. TestHighlightView показывает цветной код, но в приложении нет

**Проблема:** `.markdownBlockStyle` не применяется

**Решение:**
- Проверьте, что в `SectionCardView.swift` есть строка:
  ```swift
  .markdownBlockStyle(\.codeBlock) { configuration in
      HighlightedCodeBlock(configuration: configuration)
  }
  ```

---

## Что делать дальше

1. **Сначала**: Откройте `TestHighlightView` в Preview и проверьте, видите ли вы цветной код
2. **Если да**: Значит Highlightr работает, проблема в интеграции с MarkdownUI
3. **Если нет**: Значит проблема с самим Highlightr

**Пришлите скриншот консоли** когда откроете markdown файл - это поможет понять, что именно не работает!
